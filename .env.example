# Vercel Environment Variables
# Add these to your Vercel project settings

# Django Settings
SECRET_KEY=django-insecure-r0od)8#qrnnx61sp@9f=+fg_c(k&w!t^4*vta!3wa2posqcehe
DEBUG=False
SITE_ID=1

# Database - Vercel doesn't support SQLite, you'll need to use a cloud database
# DATABASE_URL=postgresql://user:password@host:port/database
# Or leave empty to use SQLite (not recommended for production)

# Email Settings (optional)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Session Settings
SESSION_COOKIE_SECURE=True

# Social Auth (optional)
# SOCIAL_AUTH_GOOGLE_OAUTH2_KEY=your-google-client-id
# SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET=your-google-client-secret
